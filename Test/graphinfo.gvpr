BEG_G {
    int n = nNodes($G);
    int e = nEdges($G);
    printf("There are %d nodes and %d edges in %s\n", n, e, $G.name);
	
}
N {

    printf("Node %s\n - indegree %d, outdegree %d\n", $.name, $.indegree, $.outdegree);

}